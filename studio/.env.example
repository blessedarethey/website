# Copy this file to .env for local Studio development.
# Must use the SANITY_STUDIO_ prefix — that's the only prefix the
# Studio's dev server exposes to client-side config.

SANITY_STUDIO_PROJECT_ID=s7qx1s92
SANITY_STUDIO_DATASET=production
